node_modules/

# Full-resolution originals live outside this repo entirely, in your own
# photo folder. This repo only ever holds the web-sized copies in
# images/_web/, which is what the site serves.
#
# To publish photos: run make-web.bat in your photo folder, then copy the
# _web folder it produces into images/, replacing what's there.
